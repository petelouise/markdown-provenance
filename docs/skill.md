# Markdown Provenance (MDP) — Agent Guide

This document describes the MDP syntax for marking text provenance in Markdown notes. When you create or edit a Markdown note in a vault where this convention is in use, you should apply these markers to any text you generate or modify.

---

## What it is

MDP lets every piece of text in a note carry a label indicating who produced it: an AI assistant, the human author, a quoted third party, or unknown. The plugin renders these labels as subtle background tints in Obsidian's editor and reading view. The raw markers are valid Markdown — they degrade gracefully as literal text in any other viewer.

---

## The four provenance types

| Sigil | Name | Meaning |
|---|---|---|
| `%a` | assistant | Written by an AI assistant (you) |
| `%s` | self | Written by the human author of the note |
| `%q` | quote | Reproduced from a third-party source |
| `%u` | unknown | Provenance genuinely unclear |

The boundary rule: **who produced the text**, not its relationship to a source.
- If the human paraphrases someone else → `%s`
- If you (the AI) paraphrase someone else → `%a`
- If text is reproduced verbatim or near-verbatim from a third party → `%q`

---

## Syntax

### Inline spans

```
%a{Text generated by an AI assistant.}
%s{Text written by the human author.}
%q{Text from another source.}
%u{Text of unclear origin.}
```

Spans can be nested. Inner provenance overrides outer:

```
%a{This is AI text, but %s{this correction is human}, back to AI.}
```

### Escaping

- `\%` → literal `%` (not a span opener)
- `\}` → literal `}` inside a span

### Frontmatter default

A note can declare a default provenance type in its YAML frontmatter. Text that matches the default needs no explicit marker.

```yaml
---
provenance: assistant
---
```

Valid values: `assistant`, `self`, `quote`, `unknown`.

**Example:** A note with `provenance: assistant` means all unmarked text is assumed AI-generated. Only human corrections need to be marked: `%s{this part was edited by the author}`.

---

## Rules for agents

When you write or edit a Markdown note:

1. **Wrap everything you write in `%a{...}`** unless the note's frontmatter sets `provenance: assistant`, in which case you may leave your text unmarked.

2. **Do not re-mark text that was already there.** If human-authored text (`%s{...}` or unmarked in a `provenance: self` note) exists, leave it as-is unless you are explicitly rewriting it.

3. **When you rewrite human text,** wrap your replacement in `%a{...}` so the author can see what changed.

4. **When you quote a source verbatim or near-verbatim,** use `%q{...}` regardless of who instructed you to include it.

5. **Preserve existing markers.** Never strip or flatten provenance markup that is already present.

6. **If in doubt about a passage's origin,** use `%u{...}`.

---

## Examples

### AI-drafted note with human edits

```yaml
---
provenance: assistant
---

This paragraph was generated by the AI and needs no marker.

%s{This sentence was added by the human author.}

The AI continued here, still unmarked.
```

### Human note with AI assistance

```yaml
---
provenance: self
---

This is the author's own writing.

%a{This paragraph was suggested by the AI assistant.}

Back to the author's voice. %a{One more AI-generated sentence here.}
```

### Mixed with a quote

```
The philosopher wrote: %q{The unexamined life is not worth living.}

%a{This interpretation was generated by the AI.}
```

---

## Integration

To make this guide available to your agent automatically:

- **Claude Code:** Add `@path/to/skill.md` to your vault's `CLAUDE.md`, or add an import to your user-level `~/.claude/CLAUDE.md`.
- **Other agents:** Include the contents of this file in the agent's system prompt, or reference it from whatever project-level instruction file your agent system supports (e.g. `AGENTS.md`, `.cursorrules`, `AI.md`).

The guide should be loaded whenever the agent is creating or editing Markdown files in a vault where MDP is in use.
